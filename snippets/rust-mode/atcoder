# -*- mode: snippet -*-
# name: atcoder
# key: atcoder
# --
fn main() {
    use std::io::{stdin, Read};
    let input = String::new();
    stdin().read_to_string(&mut input).expect("read_to_string");
    let mut input = input
        .split_whitespace()
        .collect::<Vec<&str>>()
        .into_iter()
        .map(|x| x.trim().parse::<u64>().unwrap())
        .collect::<Vec<u64>>();
    $0
}
